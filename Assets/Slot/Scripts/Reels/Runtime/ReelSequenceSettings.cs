namespace SlotMachine.Reels.Runtime
{
    // Intentionally empty. ReelSequenceSettings currently lives in ReelManager.cs
    // so Unity can keep using a single file definition without waiting for csproj refresh.
}
